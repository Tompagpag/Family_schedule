<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-sm-4 bg-white mt-5">
    <h2>Synchronisation des agendas</h2>
    <p>Family : <%= @family.last_name %></p>
      <div>Members:
        <% @family.family_members.each do |member| %>
          <p><%= "#{member.first_name} #{member.last_name}" %></p>
        <% end %>
      </div>

    <%= link_to 'Synchroniser avec Gmail', calendar_redirect_path, class: "btn btn-orange", style: "width=100%" %>

    </div>
  </div>
</div>
