<div class="banner d-flex align-items-center">
<div class="banner_opacity"></div>
  <div class="container">
    <h1>Family Schedule</h1>
    <p>Tout le calendrier de la famille au même endroit!</p>
      <% if user_signed_in? %>
        <% if current_user.family_member? %>
          <%= link_to "Mon Family Schedule", family_conflicts_path(current_user.family), class: "btn btn-orange" %>
        <% else %>
          <%= link_to "Créée ta famille", new_family_path, class: "btn btn-orange" %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
